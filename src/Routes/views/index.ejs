<!DOCTYPE html>
<html lang="en">
<head>
</head>
<body>
    <div class="container">
        <div class="row">
            <% books.forEach(function(book) {%>
            <div class="col-md-4">
                <div class="card">
                    <div class="card-header">
                        <%= book.tabla %>
                    </div>
                </div>
            </div>
            
            <%    }) %>
        </div>
    </div>
</body>
</html>